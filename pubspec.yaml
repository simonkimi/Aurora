name: blue_demo
description: A new Flutter application.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_blue: ^0.8.0
  shared_preferences: ^2.0.5
  flutter_mobx: ^2.0.0
  bot_toast: ^4.0.1
  flutter_colorpicker: ^0.4.0
  lottie: ^1.0.1

  flutter_svg: ^0.21.0+1
  floor: ^1.0.1
  flutter_slidable: ^0.6.0

  floating_action_bubble:
    git:
      url: https://github.com/simonkimi/floating_action_bubble.git
      ref: ^2.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.12.2
  mobx_codegen: ^2.0.1+3
  floor_generator: ^1.0.1



flutter:
  uses-material-design: true

  assets:
    - assets/lottie/
    - assets/svg/